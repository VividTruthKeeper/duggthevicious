import Container from "./Container";
import Logo from "./Logo";

const Footer = () => {
  return (
    <footer className="footer bg-BLACK">
      <Container className="flex flex-col"></Container>
    </footer>
  );
};

export default Footer;
